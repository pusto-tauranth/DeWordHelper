<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lyz.dewordhelper">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StockSelectActivity" />
        <activity android:name=".StockActivity" />
        <activity android:name=".TrainingSettingsActivity" />
        <activity android:name=".GenderTrainingActivity" />
        <activity android:name=".PlTrainingActivity" />
        <activity android:name=".WordDetailActivity" />
        <activity android:name=".ReportActivity" />
        <activity android:name=".StockDetailActivity" />
        <activity android:name=".BookSelectActivity" />
        <activity android:name=".LanguageSelectActivity" />

    </application>

</manifest>